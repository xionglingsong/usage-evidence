# Changelog

## 1.7.0（2026-09-20）

学术批改端到端特化（决策依据：typesafe-jev 结构化评估——成熟度 2.59/5，teacher 场景就绪度仅 0.34 为最大缺口，next_priority 选择整段学术批改 0.67）：

- **三条学术链前置巡逻**：结论动词链（动词强度 vs 证据类型逐句核对）、引用动词链（argues/claims/states 态度与上下文立场一致性）、结论链（future work 表达惯例），巡逻并入标记清单不单独成段
- **教师版末尾三件套**：错误类型分布表（按严重度分组）、本次最值得讲的一个点（单条，可直接当课堂例句）、给学生的自查清单（2-3 条可操作动作，对应报告实例编号）
- **证据链接学科源优先**：学术文本批改时证据指向 OpenAlex/PubMed，通用语料降为次选
- **篇幅护栏**：错误超 8 条时主体详列前 8，其余只进分布表，防止报告本身成为新的过载源

## 1.6.0（2026-09-20）

学术写作能力三项扩展（数据先行，全部锚点为本轮 OpenAlex 实测）：

- **学术批改三项专项标记**：①结论动词与证据强度错配（the results indicate/suggest 各 647/483 万篇 vs prove 27 万篇，相关性证据写 prove 是 L2 高频错）；②引用动词态度错位（argues 10,553 篇为 claims 的 4 倍，claim 自带距离感，states 纯报告）；③第一人称误纠（we demonstrate that 250 万篇主流，we/I argue 合法，真错误是 I think 进学术文）
- **学科自动推断**：未明示学科时按查询文本启发式选链路（临床词→生医，算法词→arXiv 交叉，默认总库），需要精度且无法推断才问
- **Academic Phrasebank 调用时机**：修辞功能类问题（怎么开头/怎么谨慎表达/怎么写结论）走浏览器开对应板块取候选句式，候选短语回频次源验证——Phrasebank 出候选、语料库定频次

## 1.5.1（2026-09-20）

PubMed 三态行为发现与总规律终版（自动测试四案例，querytranslation 字段揭穿）：

- **PubMed 引号短语是三态引擎**：词典命中（可信，如 play a role 178,311 篇）/ 静默词袋（count 虚高假数，如 the data are 763 万实为单词 data 命中、play a role on 80 万实为 play AND role）/ 词典外兜底（垃圾小值，如 the results suggest 返回 2 篇、as far as we know 被解析成作者名 far, as[Author] 返回 0）
- **判读法**：count 必须配 esearchresult.querytranslation——保留引号短语且量级合理才可用；「数字有区分」不再是可信判据
- **功能词组合链路定版**：OpenAlex 同值检测 → PubMed（验翻译）→ Ngram 真短语兜底（实测 play a role in 为 on 的 245 倍）
- 撤回 v1.4.1「降级走 PubMed 即可」的表述与 the data is/are 的 763 万证据（方向碰巧正确，证据坏）

## 1.5.0（2026-09-20）

出处超链接从模板建议升级为硬性验收标准（用户实测反馈：表格内部分来源仍为纯文字，不可回溯）：

- **核心纪律第 1 条扩充**：每处出处（含证据表每一行的出处列）必须写成可点击 Markdown 超链接；发送前逐行自检，任何一行纯文字先补链接再发——无链接不可回溯即不合格输出。规则前移至核心纪律，长输出尾部不漏检
- **新增来源锚链接速查表**：14 个源的锚链接构造模板一处集中，生成时照表拼接，不再依赖现场回忆
- **渲染兜底**：个别聊天 UI 表格内链接渲染缺陷的环境，降级为表内编号 + 表后来源清单，任何环境保证可回溯

## 1.4.3（2026-09-20）

短语退化攻克试验记录（字段限定变体复测）：

- arXiv `abs:`/`ti:`、Europe PMC `TITLE:`、OpenAlex `title.search` 六种变体对功能词组合全部同值退化
- 确认退化发生在索引层（停用词无位置索引），查询语法无解；PubMed 幸存因其索引保留停用词位置
- 潜在攻克方向标注：CORE API（免费 key，ES 底层）待验证

## 1.4.2（2026-09-20）

第二族学术源实测（文献 API 扩展族 + 网页搜索引擎族），排雷记录：

- **Europe PMC**：REST 与 Lucene `ABSTRACT:"..."` 字段限定对功能词组合均退化（同值）
- **Crossref**：`query.bibliographic` 模糊词袋（乱词对照组返回 8,862 条），无短语语义
- **Bing**：结果数可解析但双引号短语不执行，实测两组方向与学术语料相反，禁用作搭配频次证据
- **DDG HTML 版**（challenge 空页）、**Mojeek**（403）不可用
- **总规律**：含功能词的学术短语真短语查询目前仅 PubMed esearch 一家可用
- 新增浏览器型辅助源记录：Academic Phrasebank（修辞功能短语库）、MICUSP/MICASE（学生论文/学术口语）

## 1.4.1（2026-09-20）

学术源实测修正（四案例端到端测试发现）：

- **OpenAlex 短语匹配的退化条件**：双引号短语只对实词组合可靠（do research vs conduct research 区分清晰）；含介词/冠词/be 动词的组合静默退化为词袋，特征是两个候选返回完全相同的计数（research on/of 与 the data is/are 实测均同值），加长短语也不恢复。同值即禁用该组数字
- **降级链**：功能词组合的学术短语查证走 PubMed `[Abstract]` 真短语查询（实测 the data is 2 篇 vs the data are 7,628,735 篇）；arXiv `all:` 与 OpenAlex 三种 search 参数（search= / title_and_abstract.search / fulltext.search）对功能词组合全部退化

## 1.4.0（2026-09-20）

新增 I 类学术语域查证（回应通用语料查不了学科惯例的边界局限，Flowerdew & Petrić, 2024）：

- **OpenAlex**（主力）：约 2.5 亿文献 title+abstract 短语精确计数、`group_by=publication_year` 学术历时趋势（学术版 Ngram）、concepts 学科过滤（Google `site:` 的 API 版替代）
- **PubMed E-utilities**（生医条件化）：esearch 计数，支持 Title/Abstract 字段限定
- **arXiv API**（理工条件化）：必须 https，Atom XML totalResults
- **Google 高级检索式**（语域对比）：精确短语 + site:edu/edu.cn，浏览器专用（curl 实测为 JS 壳）；结果数为估算只做同参数量级对比；Google `*` 通配不响应，通配需求由 Linggle `_` 承担
- 降级记录：Semantic Scholar（无 key 连续 429）、Google Scholar（302 验证墙）、FLAX/BAWE（服务不可达）、SkELL（无稳定 API）
- 边界条款升级：学科惯例问题从「保守声明 + 看通用语料」改为「直接查学科源，冲突时学科源优先」

## 1.3.0（2026-09-20）

证据表出处列可跳转（来自用户实测反馈：出处是纯文字，无法点开核对）：

- **撤销 v1.1.0 的「证据表单元格内只写来源名」条款**——当时的动机是规避个别聊天 UI 的表格内链接渲染问题，但代价是所有环境下出处都不能跳转，与「把信任本 skill 变成可验证」的设计目标直接冲突
- 新规则，出处列一律写成 Markdown 超链接（来源名做锚文本，不裸露长 URL）；锚文本形式的表格链接在 Obsidian、GitHub、Typora 等主流渲染器均正常，个别 UI 渲染异常时锚文本信息仍在，不丢内容
- 模板示例表格同步改为带链接形式，示例 URL 均为真实可构造地址

## 1.2.0（2026-09-20）

查证链路 curl 化 + 跨环境验证（发现来自豆包工作模式 workbuddy 的实测，本地复核确认）：

- **Oxford curl 直连可用**：此前"TLS 指纹检测"为误判，实为短时限流；单词词条直连 200 + 完整 HTML，浏览器降为短语/习语与限流备用
- **Linggle JSON API**：GET `/api/ngram/{urlencoded 查询式}` 返回频次分布 JSON，路径传参（`?q=` 是 301 陷阱）；另有 POST `/api/example/`。整条查证链路不再依赖浏览器（约省 500MB Chromium），轻量环境可部署
- **介词查证冠词绑定技巧**：the X _ 与 a/an X _ 分开查——冠词锁定介词（the introduction 后 87.5% 是 of，an introduction 后 77.7% 是 to）
- 版本自查静默条款：版本一致时不提及

## 1.1.0（2026-09-20）

回复体验升级（来自三个假英语经典案例的实测 dogfooding）：

- 速览/深查两档：默认速览版（结论 + 1-2 条最强证据 + 替代，150 字内），语义韵/语域/辨析/批改/追问自动升级深查版
- 会话级去重：8 次接触提示、理据机制说明等只在会话内首次出现；下次自查按错误类型轮换工具
- 链接一律行内，证据表内只写来源名（修复部分聊天 UI 表格内链接渲染问题）
- 批量模式：多个疑问先出速览表再选深查，禁止逐个五段全开
- 发布后修正（21151a0）：深查触发清单补 D 类历时趋势与两个被动触发（敏感词撞表、语域错位）

## 1.0.0（2026-09-19）

首个正式版本，版本基线。

- 13 个查证源（牛津、剑桥、朗文、Linguee、Google Ngram、Linggle、Etymonline、Thesaurus.com、Urban Dictionary、FreeDictionary 等，全部实测）
- 8 类查询（搭配 / 词义 / 语域 / 历时 / 译法 / 辨析 / 搭配发现 / 语义韵）
- 批改模式（贴段即入，先标记后查证，9 类标记 + 一致性等 3 条启发式）
- 五段教学输出（结论 / 原话证据 / 语境建议 / 可模仿例句 / 辨析 + 下次自查）
- 语义韵查证（Linggle 填空看伙伴分布 + Ngram 交叉验证）
- 语域光谱三档、词源理据锚点
- 表达纪律（人话输出，认知负荷控制）
- 证据溯源链接（每条证据附真实可点的 URL）
- 运行时版本自查

设计决策与文献依据的完整对照见 [RESEARCH.md](RESEARCH.md)。
